// Placeholder QML file. Required by qt_add_qml_module to materialize the
// module directory; the actual UI lives in package/contents/ui/main.qml
// (the Plasma applet convention) and will consume this module via import.
import QtQuick

QtObject {}
